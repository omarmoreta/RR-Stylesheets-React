const styledText = {
  fontSize: "25px",
  textAlign: "right",
  margin: "10px",
  border: "25px dotted red",
};

export default function Profile() {
  return (
    <div style={styledText}>
      <p>Profile</p>
    </div>
  );
}
