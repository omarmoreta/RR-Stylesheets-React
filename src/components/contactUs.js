const styledText = {
  fontSize: "45px",
  textAlign: "center",
  margin: "100px",
  border: "3px solid blue",
};

export default function Example() {
  return (
    <div style={styledText}>
      <p>Example</p>
    </div>
  );
}
